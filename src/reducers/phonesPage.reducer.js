// // import * as R from "ramda";
// // import { phonesConstatnt } from "../constants";
// const initialState = {
//   ids: []
// };

// export default (state = initialState, { type, payload }) => {
//   switch (type) {
//     case phonesConstatnt.FETCH_PHONES_SUCCESS:
//       // const newValues = R.indexBy(R.prop("id"), payload);
//       // return
//       return Object.assign(
//         {},
//         // { ...state },
//         { ids: payload.map(id => state.ids.push(id)) }
//         // R.merge(state, {
//         //   ids: R.pluck("id", payload)
//         // })
//         // { ids: payload.map(id => state.ids.push(id)) }
//       );
//     default:
//       return { ...state };
//   }
// };
